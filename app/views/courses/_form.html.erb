<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select :location_id, options_for_select(location_collection) %>
  </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :reqs %><br>
    <%= f.text_area :reqs %>
  </div>
  <div class="field">
    <%= f.label :enrollment_cap %><br>
    <%= f.number_field :enrollment_cap %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
